# ServerDeck｜产品方案

## 一句话定位

一个面向 macOS 开发者与运维的远程服务器工作台，把 `SSH`、`SFTP`、主机管理、密钥管理、端口转发收敛到一个桌面应用里。

## 参考截图提炼出的第一版核心能力

### P0

- Host 列表与搜索
- 新建 Host
- 保存地址、端口、用户名、密码或私钥引用
- 一键发起 SSH 连接
- SFTP 双栏文件管理
- Vault / 分组 / 标签组织连接
- Known Hosts 管理
- Logs 查看

### P1

- 多标签终端
- Port Forwarding
- Snippets
- Keychain 集成
- Agent Forwarding

### P2

- mosh / serial / docker exec
- 团队共享 vault
- 同步与备份
- AI 辅助命令和运维脚本

## 页面结构

### 1. Hosts 工作台

对应你给的前两张图：

- 左侧主导航：Hosts / Keychain / Port Forwarding / Snippets / Known Hosts / Logs
- 顶部快速连接栏：支持直接输入 `ssh user@host -p 22`
- 中部主机列表：搜索、筛选、标签、分组
- 右侧详情抽屉：主机基础信息、认证方式、SSH 选项

### 2. SFTP 工作台

对应你给的后两张图：

- 左侧本地文件浏览
- 右侧远端文件区
- 无连接时显示 “Select host”
- 支持上传、下载、新建目录、删除、重命名

## 数据模型

### Host

- `id`
- `label`
- `address`
- `port`
- `username`
- `vaultId`
- `groupId`
- `tags`
- `authType`
- `passwordRef`
- `privateKeyRef`
- `agentForwarding`
- `lastConnectedAt`

### Vault

- `id`
- `name`
- `isDefault`

### Session

- `id`
- `hostId`
- `type`：`terminal | sftp | port-forward`
- `status`
- `createdAt`

## MVP 判断标准

- 能保存并编辑主机信息
- 能通过密码或私钥登录 SSH
- 能打开终端会话
- 能浏览并传输远端文件
- 整体体验比“命令行 + Finder + 多个工具”更顺

## 产品取舍

- 第一版不碰“所有协议都支持”
- 第一版不做复杂同步
- 第一版优先本地单机能力和稳定性
- 第一版界面可以先高度还原，但后端要留出真实连接边界
