# ServerDeck｜技术方案

## 结论

第一版建议采用：

`Tauri v2 + React + TypeScript + Rust`

这条路线最适合 macOS 工具型应用：

- 启动快
- 包体轻
- 本地文件系统和系统能力接入自然
- SSH / SFTP / Keychain 这类能力适合放在 Rust 侧

## 模块划分

### 前端

- `hosts`：主机列表、搜索、分组、标签
- `terminal`：会话标签页、终端视图
- `sftp`：本地/远端文件浏览、传输状态
- `settings`：主题、快捷键、默认行为

### Rust

- `host_store`：主机配置与 vault 管理
- `secret_store`：系统 Keychain 或安全存储
- `ssh_sessions`：SSH 会话生命周期
- `sftp_client`：远端文件系统操作
- `known_hosts`：已知主机校验
- `audit_log`：连接日志与错误日志

## SSH / SFTP 实现建议

### 终端

前端：

- `xterm.js`

后端：

- 优先用系统 `ssh` 做受控封装，先把稳定性跑通
- 通过 `tokio::process::Command` 管理子进程
- 只开放白名单参数，不做任意 shell 执行

这比第一天就强上纯 Rust SSH 更稳。

### SFTP

建议路线：

- P0：仍然基于系统 `ssh` + `sftp` 或受控库实现基本文件浏览/上传/下载
- P1：视稳定性切到更完整的 Rust SSH/SFTP 实现

## 安全边界

- 主机元数据可以放本地 SQLite / JSON
- 密码、私钥口令不要明文存文件
- 优先接 macOS Keychain
- `known_hosts` 校验默认开启
- 危险文件操作要二次确认

## 当前代码骨架策略

由于当前环境尚未满足完整 Tauri 启动条件，这次先交付：

- 前端页面结构
- 样式系统
- 主机表单与空状态
- Tauri 命令边界与 mock 后端

等本机工具链补齐后，再接：

1. SQLite
2. Keychain
3. SSH 会话
4. SFTP 文件操作
